# Fail a pipeline
name: example08
on: [push]
jobs:
# Sequential jobs
  Test1:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step
        id: demo
        run: exit 1
      - name: The demo step has failed
        if: ${{ failure() && steps.demo.conclusion == 'failure' }}